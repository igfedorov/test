import { ErrorCheckDirective } from './error-check.directive';

describe('ErrorCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorCheckDirective();
    expect(directive).toBeTruthy();
  });
});
